你现在扮演一个**资深产品架构师 + 系统设计师 + 平台型产品负责人 + 全栈技术负责人**。

我会提供给你两份内容：

【文档A：当前系统功能盘点文档】
→ 这是基于现有代码和界面梳理出的真实功能清单（用户端 + 管理端）

【文档B：目标产品设计方案】
→ 这是一个接近“能力平台 + 任务平台”的理想结构设计（用户端与管理端设计稿级别）

------

# 🎯 你的任务目标

请你基于【文档A】的现实基础，融合【文档B】的目标结构，输出一份：

👉 **“可直接用于开发执行的系统重构方案（工程级）”**

注意：

- 不能推翻重做
- 必须基于现有功能演进
- 要保证最小改动路径
- 要支持逐步上线
- 要支持后续能力扩展（不重启系统）

------

# 🧠 工作原则（必须遵守）

1. 以【文档A】为现实基础（当前系统真实情况）
2. 以【文档B】为目标形态（未来方向）
3. 输出必须能指导开发（不能是空泛建议）
4. 每一项改造必须说明：
   - 为什么改
   - 怎么改
   - 是否影响现有功能
5. 优先采用“渐进式重构”，不是推倒重来
6. 所有设计必须支持：
   - 能力配置化
   - 任务异步化
   - 管理后台驱动发布

------

# 📦 请按以下结构输出

------

# 1. 当前系统 vs 目标系统差异分析

请对比输出：

| 维度 | 当前系统（基于文档A） | 目标系统（基于文档B） | 差距 | 是否必须改 |
| ---- | --------------------- | --------------------- | ---- | ---------- |
|      |                       |                       |      |            |

维度至少包括：

- 用户端结构
- 管理端结构
- 接口 vs 能力模型
- 是否支持任务模型
- 是否支持配置驱动
- 发布机制
- 计费模型
- 扩展能力

------

# 2. 系统重构总体策略（必须明确）

请输出：

- 本次重构采用什么策略（例如：分层升级 / 插件化改造 / 中间层注入）
- 是否新增“任务层”
- 是否新增“能力层”
- 原有“接口层”如何保留
- 如何做到“不影响现有功能上线”

------

# 3. 用户端重构方案（工程级）

请基于【文档A用户端】：

输出：

## 3.1 页面改造映射表

| 原页面 | 现用途 | 改造后页面 | 新用途 | 是否复用 | 改造方式 |
| ------ | ------ | ---------- | ------ | -------- | -------- |
|        |        |            |        |          |          |

------

## 3.2 新增页面清单

例如：

- 能力中心
- 我的任务
- 能力详情页

每个页面必须说明：

- 页面目标
- 核心模块
- 数据来源
- 是否复用组件

------

## 3.3 用户主流程改造

从：
API调用 →

升级为：

能力选择 → 任务提交 → 任务查询 → 结果查看

请输出完整流程

------

# 4. 管理端重构方案（重点）

请基于【文档A管理端】：

输出：

## 4.1 管理模块改造映射

| 原模块 | 当前作用 | 新模块 | 新作用 | 是否保留 |
| ------ | -------- | ------ | ------ | -------- |
|        |          |        |        |          |

------

## 4.2 必须新增模块

必须包含：

- 能力管理
- 执行器管理
- 任务运营
- 发布系统

每个模块说明：

- 核心功能
- 页面结构
- 数据结构
- 与现有系统关系

------

## 4.3 能力发布机制设计（重点）

必须输出：

👉 如何做到：

- 新增能力不用改代码
- 不用重启系统
- 后台配置 → 前台自动生效

需要明确：

- ability schema
- executor binding
- UI schema
- 发布流程

------

# 5. 核心数据模型升级（工程级）

请输出：

👉 在当前数据库基础上：

需要新增哪些表：

- ability
- ability_version
- job
- executor
- job_result
- job_log

并说明：

- 每个表作用
- 与现有表关系
- 是否需要迁移数据

------

# 6. API/接口层升级方案

请说明：

👉 当前 API 如何升级为：

- /jobs（任务模型）
- ability 查询接口
- job 查询接口
- callback机制

并说明是否兼容旧接口

------

# 7. 功能改造优先级（非常关键）

请输出：

## 第一阶段（必须改）

- 哪些页面/模块必须先改

## 第二阶段（增强）

- 哪些是体验优化

## 第三阶段（平台化）

- 哪些是未来扩展能力

------

# 8. 最终“开发任务拆解表”（重点输出）

请输出表格：

| 模块 | 功能点 | 所属端 | 改造类型 | 优先级 | 是否影响现网 | 开发复杂度 | 备注 |
| ---- | ------ | ------ | -------- | ------ | ------------ | ---------- | ---- |
|      |        |        |          |        |              |            |      |

要求：

- 粒度尽量细
- 能直接用于排期
- 能分配给开发

------

# 9. 风险与注意事项

请说明：

- 哪些改动风险最高
- 哪些地方容易破坏现有系统
- 如何做灰度上线
- 如何保证不中断服务

------

# 📌 输出要求

- 必须结构化
- 必须可执行
- 必须基于文档A现实
- 不允许空泛建议
- 不允许“重新设计一套系统”
- 必须强调“渐进式改造路径”

请把第8部分“开发任务拆解表”细化到可以直接分配给开发的粒度，每个功能点尽量独立。