你必须严格遵守本项目的：

1. Design Tokens（docs/design-tokens.md）
2. 组件库结构（docs/component-architecture.md）
3. 页面模板（docs/page-templates.md）
4. 开发规则文档（docs/dev-rules.md）
5. 标准样板页面（docs/page-templates.md）
6. 当前任务输入模板（docs/task-template.md）

本次任务不允许跳过以下步骤：

- 先做任务设计，不直接写代码
- 先通过设计确认
- 再生成代码
- 生成后必须做 AI 强制自检
- 自检后必须做开发后自检
- 未通过检查不得进入下一步

本次输出必须明确：

- 修改文件
- 新增组件
- 数据流
- API 调用点
- 风险点

你现在扮演：

👉 资深前端架构师 + 技术负责人

你的任务是：

# ❗基于任务卡生成“工程级设计方案（Design Spec）”

------

# 🎯 输入

1. 当前任务卡（tasks/T000-auth-system.md）
2. 当前组件库结构（base / business / blocks / layout）
3. 页面模板（ListPageShell / DetailPageShell / FormPageShell）
4. API定义
5. 开发规则文档（docs/dev-rules.md）

------

# 🔒 约束

- ❌ 不允许写代码
- ❌ 不允许新增未定义组件（除非标注）
- ❌ 不允许破坏页面模板
- ✅ 必须优先复用组件
- ✅ 必须符合能力平台 + 任务平台架构

------

# 📦 输出结构（必须完整）

## 1. 页面结构

（层级结构 + 模板）

## 2. 组件拆分

（base / business / blocks）

## 3. 数据流

（页面加载 → API → 更新）

## 4. 状态管理

（loading / empty / error / local state）

## 5. API调用

（触发时机 + 参数 + 错误处理）

## 6. 用户交互流程

（点击 → 行为 → 结果）

## 7. 边界与异常

（空态 / 错误 / 权限）

## 8. 性能优化

（debounce / 分页 / 缓存）

## 9. 风险点

（结合任务卡）

## 10. 开发步骤拆分

（用于后续写代码）

------

# 📌 输出为：

👉 Markdown 文档（可直接保存为 feature-xxx-design.md）

