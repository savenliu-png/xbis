你必须严格遵守本项目的：

1. Design Tokens（docs/design-tokens.md）
2. 组件库结构（docs/component-architecture.md）
3. 页面模板（docs/page-templates.md）
4. 开发规则文档（docs/dev-rules.md）
5. 标准样板页面（docs/page-templates.md）
6. 当前任务输入模板（docs/task-template.md）

本次任务不允许跳过以下步骤：

- 先做任务设计，不直接写代码
- 先通过设计确认
- 再生成代码
- 生成后必须做 AI 强制自检
- 自检后必须做开发后自检
- 未通过检查不得进入下一步

本次输出必须明确：

- 修改文件
- 新增组件
- 数据流
- API 调用点
- 风险点

你现在扮演一个**资深产品架构师 + 技术负责人 + 项目经理（PM）**。

我将提供：

1. 重构方案（01 xbis-front 系统重构方案.md）
2. 已有任务示例（如 feature-ability-center.md、feature-billing-upgrade.md、feature-task-platform.md）
3. 当前项目背景

------

# 🎯 你的任务目标

请你基于已有方案和任务示例，输出一套完整的：

👉 **任务池（Sprint Backlog + Milestone Backlog）**

要求：

- 每个任务必须可以“独立开发 + 独立测试 + 独立上线”
- 粒度必须达到“工程任务级”，不能只是页面名称
- 必须覆盖：用户端 + 管理端 + 后端 + 基础能力层

------

# 📦 输出结构要求

------

# 1. 系统模块划分（先做这一步）

请先把整个系统拆成模块，例如：

- 用户端（能力平台）
- 用户端（任务系统）
- 用户端（商业化）
- 管理端（能力管理）
- 管理端（任务运营）
- 管理端（执行器管理）
- 后端 API 层
- 数据模型层
- 基础设计系统（如组件/模板）

每个模块说明：

- 模块目标
- 是否必须（P0/P1/P2）

------

# 2. Sprint Backlog（任务池，核心输出）

请按以下格式输出所有任务：

| 任务编号 | 功能名称 | 所属端 | 所属模块 | 页面/路由 | 依赖组件 | API | 优先级 | 是否影响现网 | 前置任务 | 简要说明 |

要求：

1. 每一行是一个“可开发任务”
2. 功能名称必须具体（例如：任务列表页，而不是任务系统）
3. API 要明确（如 `/jobs`, `/abilities/:id`）
4. 前置任务必须引用任务编号（形成依赖链）
5. 优先级：
   - P0：必须先做，否则系统不可运行
   - P1：增强体验
   - P2：扩展能力
6. 至少拆出：
   - 20～40 个任务（不要少于20）
7. 必须覆盖：
   - 能力中心
   - 能力详情
   - 能力测试
   - 能力接入
   - 任务中心
   - 任务执行（job）
   - 任务列表
   - 任务详情
   - 管理端能力管理
   - 管理端任务运营
   - 执行器管理
   - 套餐与计费

------

# 3. 任务依赖关系说明（用于开发顺序）

请输出：

## 3.1 关键依赖链（必须做）

例如：

- 能力中心 → 依赖 → abilities API
- 任务列表 → 依赖 → job API

## 3.2 可并行开发任务组

例如：

- UI层（能力列表 + 能力详情）可并行
- 后端 API 层可独立开发

## 3.3 阻塞任务（当前不能开始的）

例如：

- 未完成 API
- 未完成组件库

------

# 4. 推荐开发顺序（非常关键）

请输出一个清晰顺序，例如：

Step 1：基础层（API / 数据模型）
Step 2：能力平台（能力中心 + 详情）
Step 3：任务系统（job + 任务列表）
Step 4：能力接入与商业化
Step 5：管理端

------

# 5. Milestone Backlog（版本规划）

请按版本划分任务：

## Milestone 1（MVP上线）

必须包含：

- 能力浏览
- 能力详情
- 基础任务执行
- 任务列表
- 基础计费

## Milestone 2（增强版）

- 能力测试
- 能力接入
- 更完整任务运营

## Milestone 3（平台化）

- 执行器管理
- 第三方接入
- 能力市场优化

每个 Milestone 输出：

- 包含哪些任务编号
- 上线目标

------

# 📌 输出要求

- 必须结构化
- 不允许只给概念
- 必须可直接用于开发排期
- 必须标清依赖关系
- 必须符合“能力平台 + 任务平台”架构
- 优先复用已有任务示例（如 ability-center）

------

# ⚠️ 重要限制

- 不允许只拆页面，必须拆到：
  - API层
  - 数据层
  - UI层
- 不允许遗漏“任务系统（job）”
- 不允许遗漏“管理端能力”
- 每个任务必须能进入开发循环（Phase C）
- 任务池文件需要落到文档中