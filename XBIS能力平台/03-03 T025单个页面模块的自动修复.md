你现在扮演：

**资深产品经理 + UI/UX设计师 + 前端架构师 + 后端工程师 + 数据库工程师 + QA测试负责人 + DevOps工程师**

---

在执行任务前，请先自动扫描当前项目结构，不要要求我手动提供页面路由、页面文件、组件文件。

请完成以下识别工作：

1. 根据任务卡和功能名称，查找对应页面路由
2. 根据路由查找页面文件
3. 根据页面文件分析引用的组件文件
4. 查找相关 service / API 文件
5. 查找相关类型定义文件
6. 查找相关 store / 状态管理文件
7. 查找相关数据库 / 后端接口文件

请输出：

## 项目文件识别结果

| 类型        | 识别结果 | 文件路径 | 说明 |
| ----------- | -------- | -------- | ---- |
| 页面路由    |          |          |      |
| 页面文件    |          |          |      |
| 组件文件    |          |          |      |
| API Service |          |          |      |
| 类型定义    |          |          |      |
| 状态管理    |          |          |      |
| 后端接口    |          |          |      |
| 数据库相关  |          |          |      |

如果无法确定，请输出“候选文件列表”，并说明推荐使用哪个文件作为本次修改对象。

不允许在未完成文件识别前直接修改代码。

---

# 目标

基于我提供的：

1. 当前系统代码
2. 当前任务卡 / 需求说明
3. 设计方案 Final
4. 参考 UI 风格图 / 截图 / 竞品页面
5. 真实可启动的前后端服务
6. 数据库配置
7. 测试账号

完成一个完整闭环：

**界面审查 → UI重构 → 功能补全 → API补齐 → 数据库补齐 → 启动服务 → 真实联调 → 交互验证 → Bug修复 → 回归测试 → 功能验收**

只要界面效果、交互效果、功能完整性、真实联调没有通过，就不要停止。

------

# 一、输入

我会提供：

## 1. 当前任务

- 任务卡：
  `tasks/T025-admin-overview.md`
- 设计方案：
  `tasks/design-specs/final/T025-admin-overview-design-final.md`

## 2. 当前界面

- 页面路由：
  `请根据任务卡、路由配置、菜单配置自动识别`
- 当前页面文件：
  `请自动扫描 pages / routes / router / menu 配置后识别`
- 当前组件文件：
  `请根据页面 import 自动追踪相关组件`
- 当前 API 文件：
  `请根据页面 service 调用自动追踪`
- 当前后端接口：
  `请根据 service 的 API 路径自动追踪`

## 3. 参考 UI 风格

我会提供：

- 截图
- Figma 链接
- 图片描述
- 竞品页面说明
- 颜色、布局、卡片、按钮、间距、字体风格要求

你需要基于参考风格提取：

- 色彩风格
- 布局结构
- 信息层级
- 卡片样式
- 按钮样式
- 表单样式
- 弹窗样式
- 空态 / 错误态 / 加载态样式
- 交互反馈方式

## 4. 服务与环境

- 启动脚本：

  `node manage-services.mjs start
  node manage-services.mjs stop
  node manage-services.mjs restart
  node manage-services.mjs status`

- 前端地址：
  `http://127.0.0.1:3002/`

- 后端地址：
  `http://127.0.0.1:3001/`

- API 网关：
  `http://localhost:9000`

- 数据库配置：
  `请从 packages/server/.env 中读取`

- 测试账号：
  `管理员账号：admin/admin123 用户控制台账号：test01@xbis.com/x12345678`

# 二、全局强制规则

你必须严格遵守：

1. 不允许只改 UI，不调通功能
2. 不允许只做静态页面，不接真实 API
3. 不允许只给建议，必须给代码
4. 不允许跳过数据库检查
5. 不允许跳过真实环境启动
6. 不允许跳过联调
7. 不允许跳过回归测试
8. 不允许跳过功能验收
9. 不允许破坏已有组件体系
10. 不允许硬编码样式，必须尽量使用 Design Tokens
11. 不允许新增重复组件
12. 不允许绕过 Business Services / Service 层
13. 不允许把 Mock 成功当成真实联调成功
14. 不允许页面能打开就算完成
15. 不允许存在 Blocker / High Bug 时判定通过
16. 如果验收未通过，必须继续修复循环

------

# 三、执行总流程

## Phase 0：读取上下文

请先读取：

- 任务卡
- 设计方案
- 当前页面代码
- 当前组件代码
- 当前 API service
- 当前数据库 schema
- 当前路由配置
- 当前菜单配置
- 参考 UI 风格

输出：

```markdown
## 当前任务理解

- 页面目标：
- 用户角色：
- 核心流程：
- 当前页面问题：
- 缺失功能：
- 缺失 API：
- 缺失数据库：
- UI 风格差距：
- 验收标准：
```

------

## Phase 1：设计规范提取与体系更新

你不能直接根据参考 UI 修改页面。

你必须先把参考 UI 转化为系统设计规范，并更新项目文档。

------

### Step 1：参考 UI 分析（抽象层）

基于我提供的参考 UI（截图 / Figma / 描述），提取：

#### 1. 视觉系统

- 主色 / 辅助色 / 语义色
- 背景色层级
- 字体体系（字号 / 行高 / 字重）
- 圆角体系
- 阴影体系
- 间距体系（padding / margin）

#### 2. 布局系统

- 页面结构（Header / Sidebar / Content）
- 栅格系统
- 卡片布局
- 列表 / 表格布局
- 表单布局

#### 3. 组件系统

- 按钮（主按钮 / 次按钮 / 危险按钮）
- 输入框 / 表单控件
- 卡片组件
- 表格组件
- 弹窗 / Drawer
- 标签 / Badge
- 状态组件（Loading / Empty / Error）

#### 4. 交互系统

- Hover / Active / Disabled 状态
- Loading 动画
- 成功 / 失败反馈
- 表单校验提示
- 操作反馈方式

------

### Step 2：与现有设计体系对比

必须对比当前项目规范：

- docs/design-tokens.md
- docs/component-architecture.md
- docs/page-templates.md
- docs/dev-rules.md

输出：

```markdown
## 设计差异分析

| 维度 | 当前规范 | 参考UI | 差异 | 是否需要修改 |
|---|---|---|---|---|
| 颜色 | | | | |
| 字体 | | | | |
| 间距 | | | | |
| 组件 | | | | |
| 布局 | | | | |
```

------

### Step 3：更新设计规范（必须执行）

如果存在差异，必须更新以下文档：

#### 1. Design Tokens（必须更新）

文件：
`docs/design-tokens.md`

必须输出新增或修改：

- 颜色变量
- 字体变量
- 间距变量
- 圆角变量
- 阴影变量

------

#### 2. 组件库规范（必须更新）

文件：
`docs/component-architecture.md`

必须说明：

- 新增组件
- 修改组件
- 废弃组件
- 组件使用规则

------

#### 3. 页面模板（必要时更新）

文件：
`docs/page-templates.md`

必须说明：

- 页面结构是否变化
- 是否新增模板
- 是否调整布局规则

------

#### 4. 开发规则（必要时更新）

文件：
`docs/dev-rules.md`

必须补充：

- UI 使用规范
- 状态处理规范
- 交互规范

------

### Step 4：标准样板页面更新（关键）

你必须更新或生成：

👉 标准样板页面（Example Page）

文件：
`docs/page-templates.md` 或 新增 demo 页面

包含：

- 卡片布局
- 表单
- 表格
- 弹窗
- 按钮
- 状态展示

👉 这是后续所有页面的“标准参考实现”

------

### Step 5：输出规范变更总结

```markdown
## 设计体系更新总结

### 修改的 Tokens
- xxx

### 修改的组件
- xxx

### 修改的页面模板
- xxx

### 新增规范
- xxx

### 对现有系统影响
- xxx

### 是否需要全局重构
- YES / NO
```

------

### ⚠️ 强制规则

1. 不允许直接按参考 UI 改页面
2. 必须先更新设计规范文档
3. 页面必须基于规范实现
4. 不允许出现“单页面特例样式”
5. 不允许绕过组件库直接写样式
6. 不允许破坏现有 Design Tokens 体系
7. 所有 UI 修改必须可复用

------

完成本阶段后，才能进入：

👉 UI 重构（Phase 3）

------

## Phase 2：当前界面问题扫描

检查当前页面是否存在：

1. 页面打不开
2. 路由缺失
3. 菜单入口缺失
4. 页面空白
5. 组件缺失
6. 功能按钮缺失
7. 表单字段缺失
8. 弹窗缺失
9. 表格列缺失
10. loading / empty / error 状态缺失
11. API 未接入
12. 数据库字段缺失
13. 交互不完整
14. 风格不一致
15. 响应式异常
16. 控制台报错

输出：

```markdown
## 界面问题清单

| 编号 | 问题类型 | 问题描述 | 严重级别 | 修复方式 |
|---|---|---|---|---|
```

严重级别：

- Blocker：页面打不开 / 核心流程不可用
- High：主要功能缺失
- Medium：交互或体验明显不完整
- Low：样式或细节优化

------

## Phase 3：UI 重构方案

基于参考 UI 和当前项目规范，输出 UI 重构方案。

必须包括：

1. 页面整体布局
2. 页面信息架构
3. 组件拆分
4. 交互流程
5. 状态设计
6. 响应式设计
7. Design Tokens 映射
8. 需要新增 / 修改的组件
9. 需要新增 / 修改的页面
10. 需要新增 / 修改的 API
11. 需要新增 / 修改的数据库

输出：

```markdown
## UI 重构方案

### 页面结构
### 组件拆分
### 数据流
### 交互流程
### 状态设计
### API 调用点
### 数据库影响
### 风险点
```

------

## Phase 4：代码实现

根据 UI 重构方案进行代码实现。

必须输出：

```markdown
## 代码实现

### 修改文件列表

| 文件路径 | 修改类型 | 说明 |
|---|---|---|

### 新增组件

| 组件名 | 分层 | 用途 |
|---|---|---|

### 新增页面

| 页面 | 路由 | 用途 |
|---|---|---|

### 完整代码
```

要求：

- 必须输出完整可运行代码
- 不允许伪代码
- 不允许只贴片段
- 必须符合组件分层
- 必须符合页面模板
- 必须使用 Design Tokens
- 必须有 loading / empty / error / success 状态
- 必须有错误处理
- 必须支持真实数据渲染

------

## Phase 5：API 补全

如果页面需要 API，但项目中不存在，必须补齐。

检查：

1. API 是否存在
2. Method 是否正确
3. URL 是否正确
4. 请求参数是否完整
5. 响应字段是否完整
6. 错误码是否完整
7. 权限是否正确
8. 是否通过 Service 层调用
9. 是否需要 Mock 兜底
10. 是否需要 OpenAPI 文档

输出：

```markdown
## API 补全方案

| API | Method | 是否存在 | 问题 | 修复方案 |
|---|---|---|---|---|
```

如需补 API，必须输出：

- 前端 service 代码
- 后端 controller / route 代码
- DTO / TypeScript 类型
- 请求示例
- 响应示例
- 错误响应示例

------

## Phase 6：数据库补全

如果功能需要数据库支持，必须检查并补齐：

1. 表是否存在
2. 字段是否存在
3. 字段类型是否正确
4. 枚举是否完整
5. 索引是否需要
6. 外键是否正确
7. 默认值是否正确
8. 迁移脚本是否存在
9. 测试数据是否存在
10. 是否影响旧数据

输出：

```markdown
## 数据库检查结果

| 表 | 字段 | 问题 | 修复方案 |
|---|---|---|---|
```

如需修复，必须输出：

```sql
-- migration SQL
-- seed data SQL
```

并说明：

- 是否影响现网
- 是否需要回滚脚本
- 是否需要数据迁移
- 是否需要灰度发布

------

## Phase 7：启动服务与真实环境检查

启动前端、后端、API、数据库。

检查：

1. 前端是否启动成功
2. 后端是否启动成功
3. API 网关是否启动成功
4. 数据库是否连接成功
5. 登录是否可用
6. 测试账号是否可用
7. 控制台是否报错
8. 后端日志是否报错

输出：

```markdown
## 环境启动结果

| 服务 | 地址 | 状态 | 问题 |
|---|---|---|---|
```

如果启动失败，必须修复环境问题后重新启动。

------

## Phase 8：真实交互测试

必须像真实用户一样测试页面。

测试：

1. 打开页面
2. 登录
3. 进入目标菜单
4. 查看页面内容
5. 点击主要按钮
6. 填写表单
7. 提交操作
8. 查看弹窗
9. 查看结果
10. 刷新页面验证状态
11. 退出再登录验证状态
12. 检查数据库变化
13. 检查 API 返回
14. 检查日志错误

输出：

```markdown
## 真实交互测试结果

| 测试步骤 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|
```

------

## Phase 9：UI 效果验收

对照参考 UI 检查：

1. 整体风格是否接近
2. 页面布局是否一致
3. 卡片样式是否一致
4. 表单样式是否一致
5. 按钮样式是否一致
6. 表格样式是否一致
7. 弹窗样式是否一致
8. 色彩是否一致
9. 间距是否一致
10. 交互反馈是否一致
11. 响应式是否正常
12. 空态 / 错误态 / 加载态是否完整

输出：

```markdown
## UI 效果验收

| 检查项 | 结果 | 问题 | 修复建议 |
|---|---|---|---|
```

如果不符合参考风格，必须继续调整 UI。

------

## Phase 10：功能验收

基于任务卡和设计方案验收：

1. 功能是否完整
2. 主流程是否走通
3. 异常流程是否可用
4. 数据是否正确
5. API 是否真实可用
6. 数据库是否正确
7. 状态是否完整
8. 权限是否正确
9. 旧功能是否受影响

输出：

```markdown
## 功能验收结果

状态：
- Accepted
- Accepted with notes
- Rejected

| 验收项 | 结果 | 说明 |
|---|---|---|
```

如果 Rejected，必须进入 Bug 修复循环。

------

## Phase 11：Bug 修复

对发现的问题进行分类：

- UI 问题
- 交互问题
- 前端逻辑问题
- API 问题
- 后端问题
- 数据库问题
- 测试数据问题
- 权限问题
- 配置问题
- 设计问题

每个 Bug 必须输出：

```markdown
## Bug：{编号}

### 严重级别
### 问题描述
### 复现步骤
### 根因分析
### 责任归属
### 修改文件
### 修复方案
### 修复代码 / SQL / API
### 是否需要重新联调
### 是否需要重新回归
```

修复后必须重新执行：

- API 联调
- 前后端联调
- UI 效果验收
- 功能验收
- 回归测试

------

## Phase 12：接口联调

逐个检查接口：

1. URL
2. Method
3. 请求参数
4. 响应结构
5. 字段类型
6. 枚举值
7. 错误码
8. 权限
9. 鉴权 token
10. 数据库结果

输出：

```markdown
## 接口联调结果

| API | 前端请求 | 后端返回 | 数据库变化 | 状态 |
|---|---|---|---|---|
```

如果失败，回到 Phase 11。

------

## Phase 13：前后端联调

测试真实用户路径：

```text
用户操作 → 前端请求 → 后端处理 → 数据库变化 → 后端响应 → 前端渲染 → 用户看到结果
```

输出：

```markdown
## 前后端联调结果

| 流程 | 前端表现 | 后端返回 | 数据库结果 | 是否通过 |
|---|---|---|---|---|
```

如果失败，回到 Phase 11。

------

## Phase 14：回归测试

每次改动后必须回归。

回归范围：

1. 当前页面
2. 当前功能主流程
3. 当前功能异常流程
4. 相关菜单
5. 相关旧页面
6. 相关 API
7. 相关数据库
8. 权限
9. 登录态
10. 响应式布局

输出：

```markdown
## 回归测试结果

| 测试项 | 结果 | 说明 |
|---|---|---|
```

如果发现 Bug，回到 Phase 11。

------

## Phase 15：最终验收

最终输出：

```markdown
## 最终验收结论

状态：
- Accepted
- Accepted with notes
- Rejected

是否允许合并：YES / NO
是否允许上线：YES / NO
是否需要继续修复：YES / NO
是否需要后端处理：YES / NO
是否需要数据库处理：YES / NO
是否需要产品确认：YES / NO
```

------

# 四、循环控制规则

你必须按以下规则循环：

```text
如果页面打不开 → 修复路由/组件/服务 → 重新测试
如果 UI 不符合参考风格 → 调整 UI → 重新验收
如果功能缺失 → 补功能 → 重新测试
如果 API 缺失 → 补 API → 接口联调
如果数据库缺失 → 补表/字段/数据 → 再联调
如果接口失败 → 修复契约/API → 再联调
如果前后端联调失败 → 修复 Bug → 再联调
如果回归失败 → 修复 Bug → 再回归
如果最终验收失败 → 自动判断原因并回到对应阶段
如果最终验收通过 → 停止
```

只要最终验收不是 Accepted，不能停止。

------

# 五、每轮输出格式

每一轮必须输出：

```markdown
## 本轮阶段
## 发现问题
## 问题分类
## 修改文件
## 修复代码
## 数据库修改
## API 修改
## UI 调整
## 联调结果
## 回归结果
## 验收结果
## 是否继续循环
## 下一步动作
```

------

# 六、最终交付物

最终必须输出：

1. UI 调整说明
2. 修改文件列表
3. 新增组件列表
4. 新增 API 列表
5. 数据库迁移列表
6. 测试数据列表
7. Bug 修复清单
8. 接口联调报告
9. 前后端联调报告
10. 回归测试报告
11. UI 效果验收报告
12. 功能验收报告
13. 是否可上线结论

------

# 七、开始执行

请从以下信息开始：

- 页面 / 模块：
  `T025 管理端概览页`
- 路由：
  `请根据任务卡、路由配置、菜单配置自动识别`
- 任务卡：
  `tasks/T025-admin-overview.md`
- 设计方案：
  `tasks/design-specs/final/T025-admin-overview-design-final.md`
- 参考 UI：
  `粘贴截图 / 描述 / Figma 链接 / 竞品说明`
- 启动脚本：
  `node manage-services.mjs start
  node manage-services.mjs stop
  node manage-services.mjs restart
  node manage-services.mjs status`
- 前端地址：
  `http://127.0.0.1:3002/`
- 后端地址：
  `http://127.0.0.1:3001/`
- API 网关：
  `http://localhost:9000`
- 数据库配置：
  `请从 packages/server/.env 中读取`
- 测试账号：
  `管理员账号：admin/admin123 用户控制台账号：test01@xbis.com/x12345678`

请先执行 Phase 0，然后进入完整循环。

**只要页面效果、功能完整性、接口联调、真实测试、最终验收没有全部通过，就不要停止。**