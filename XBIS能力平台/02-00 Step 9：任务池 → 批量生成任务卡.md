你必须严格遵守本项目的：

1. Design Tokens（docs/design-tokens.md）
2. 组件库结构（docs/component-architecture.md）
3. 页面模板（docs/page-templates.md）
4. 开发规则文档（docs/dev-rules.md）
5. 标准样板页面（docs/page-templates.md）
6. 当前任务输入模板（docs/task-template.md）

本次任务不允许跳过以下步骤：

- 先做任务设计，不直接写代码
- 先通过设计确认
- 再生成代码
- 生成后必须做 AI 强制自检
- 自检后必须做开发后自检
- 未通过检查不得进入下一步

本次输出必须明确：

- 修改文件
- 新增组件
- 数据流
- API 调用点
- 风险点

你现在扮演：

👉 **资深产品经理 + 技术负责人 + 工程化交付负责人**

你的任务是：

# ❗将 Sprint Backlog 中的所有任务，批量转换为标准“任务卡文件（task card）”

------

# 🎯 目标

基于：

- Sprint Backlog（任务池）
- 任务卡模板（task-template.md）

输出：

👉 每个任务对应一个完整的 Markdown 任务卡文件内容

------

# 📦 输入数据

## 1️⃣ Sprint Backlog

（文档：sprint-backlog.md）

## 2️⃣ 任务卡模板

（文档：task-template.md）

------

# 🔄 执行规则（非常重要）

1. 必须逐条处理任务（T001 ~ T036）
2. 每个任务生成一个完整任务卡
3. 严格遵循 task-template.md 结构
4. 所有 ⭐ 字段必须填写
5. 不允许留空字段（未知则标注 TODO）
6. 必须补充合理的：
   - API 示例
   - 数据结构（TypeScript）
   - 交互流程
   - 验收标准
7. 必须结合任务池信息进行推断（不能空泛）

------

# 📁 输出格式（必须严格）

对每一个任务输出：

------

## 📄 文件名

```
tasks/T{编号}-{功能名称英文}.md
```

------

## 📄 文件内容

```markdown
（完整任务卡内容，严格按照模板结构）
```

------

# 🧠 字段映射规则（关键）

请按以下规则填充：

| Sprint字段       | 模板字段           |
| ---------------- | ------------------ |
| 功能名称         | 功能名称 ⭐         |
| 任务编号（Txxx） | 任务编号           |
| 所属模块         | 所属模块 ⭐         |
| 页面/路由        | 页面位置 ⭐         |
| API              | API 接口 ⭐         |
| 依赖组件         | 依赖组件 ⭐         |
| 前置任务         | 是否依赖其他任务 ⭐ |
| 是否影响现网     | 是否影响现网 ⭐     |
| 优先级           | 优先级             |

------

# 🧩 额外补充要求

对于每个任务，必须补充：

## 1️⃣ 数据结构

- 给出 TypeScript interface
- 与 ability/job/executor 对齐

## 2️⃣ API

- 补充 Method / Path / Request / Response
- 给出示例 JSON

## 3️⃣ 交互流程

- 主流程（必须）
- 异常流程（至少1条）

## 4️⃣ 验收标准

- 至少 5 条功能验收
- 必须具体（可验证）

------

# ⚠️ 限制

- ❌ 不允许输出解释
- ❌ 不允许跳过任务
- ❌ 不允许只写部分字段
- ❌ 不允许省略模板结构

------

# 🚀 输出策略（避免过长）

由于任务较多：

👉 每次输出 3 个任务卡
👉 输出后暂停，等待我输入“继续”

------

# 🧠 开始执行

请从：

👉 T001 开始生成，循环生成任务卡并落地成文档，直到36个任务卡文档全部落地。
