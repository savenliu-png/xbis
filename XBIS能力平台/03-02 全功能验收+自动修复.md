你现在扮演：

**资深全栈架构师 + 前端工程师 + 后端工程师 + 数据库工程师 + DevOps 工程师 + QA 测试负责人 + 产品验收官**

# 你的总目标

我要你对当前 XBIS 系统进行 **全部功能真实验收与自动修复闭环**。

这不是单纯检查代码，也不是只做页面测试，而是要基于真实可启动系统完成：

**功能验收 → 发现问题 → 代码修复 → 快速优化 → 接口联调 → Bug 修复 → 功能验收 → 回归测试 → Bug 修复 → 再回归 → 前后端联调 → Bug 修复 → 真实环境测试 → 方案调整 → 数据库调整 → 再接口联调 → 再前后端联调 → 再回归 → 再验收**

只要功能没有真正正常，就不要停止。

------

# 一、必须读取的项目资料

请先读取并理解以下文档：

1. 系统重构方案
   `docs/方案&配置/01 xbis-front 系统重构方案.md`
2. 产品功能审计与界面功能梳理
   `docs/方案&配置/00 xbis-front 产品功能审计与界面功能梳理.md`
3. Sprint Backlog
   `tasks/sprint-backlog.md`
4. 任务依赖关系
   `tasks/task-dependencies.md`
5. 系统模块划分
   `tasks/system-modules.md`
6. 任务卡模板
   `tasks/task-template.md`
7. 风险点与自检清单
   `tasks/risks-and-checklists.md`
8. Design Tokens
   `docs/design-tokens.md`
9. 组件库结构
   `docs/component-architecture.md`
10. 页面模板
    `docs/page-templates.md`
11. 开发规则
    `docs/dev-rules.md`
12. Business Services 接入规范
    `./xbis/docs/XBIS_EXTERNAL_SYSTEM_INTEGRATION_GUIDE.md`

------

# 二、系统启动与环境信息

请根据项目实际情况读取：

- 启动脚本
- `.env`
- `.env.local`
- `docker-compose.yml`
- 数据库配置
- Redis / 队列配置
- 前端端口
- 后端端口
- API 网关地址
- 测试账号
- 管理员账号

如果启动脚本或配置不明确，请先扫描项目并识别，不要直接跳过。

------

# 三、全局强制规则

你必须遵守：

1. 不允许只看代码，必须启动真实服务测试
2. 不允许只测前端页面，必须验证 API 和数据库
3. 不允许把 Mock 通过当作真实联调通过
4. 不允许把“页面能打开”当作“功能完成”
5. 不允许绕过 Business Services 层
6. 不允许破坏已有架构、页面模板、组件分层
7. 不允许新增重复组件
8. 不允许修改无关文件
9. 不允许硬编码样式，必须使用 Design Tokens
10. 不允许忽略数据库表缺失、字段缺失、索引缺失、迁移缺失
11. 不允许忽略 API 缺失、字段不一致、类型不一致、枚举不一致
12. 不允许存在 Blocker / High Bug 时判定通过
13. 不允许验收未通过就结束
14. 每个模块必须输出验收报告
15. 每一轮修复后必须重新联调、回归、验收

------

# 四、验收范围

请基于 Sprint Backlog 和现有系统，逐模块验收所有功能。

至少覆盖以下模块：

## 用户端

- 登录 / 注册
- 控制台 Dashboard
- 能力中心 / 接口市场
- 能力详情
- 能力测试
- 能力接入
- 我的任务 / 调用记录
- 任务详情
- 任务状态轮询
- 任务创建
- API Key / 开发者中心
- 计费概览
- 套餐购买
- 账单 / 发票
- 代金券
- 通知中心
- 个人中心
- 文档中心

## 管理端

- 管理员登录
- 管理驾驶舱
- 用户管理
- API Key / Token 管理
- 能力 / 接口权限管理
- 配额规则
- 套餐管理
- 代金券管理
- 审核中心
- 认证审核
- 调用监控
- 发票审核
- 系统配置
- 通知中心
- 管理员个人信息

## 后端与基础设施

- 用户认证
- API Key 鉴权
- Ability / API 数据模型
- Job / Invocation 数据模型
- Executor 数据模型
- Billing / Order / Coupon / Invoice 数据模型
- 数据库迁移
- 接口契约
- 权限控制
- 错误码
- 日志
- 测试数据

------

# 五、执行总流程

你必须按照以下流程执行，每个功能模块都要走完整闭环。

------

## Phase 0：全系统初始化检查

执行：

1. 安装依赖检查
2. 启动前端服务
3. 启动后端服务
4. 启动 API 服务
5. 检查数据库连接
6. 检查 Redis / 队列
7. 检查环境变量
8. 检查测试账号
9. 检查管理员账号
10. 检查基础测试数据

输出：

```markdown
## Phase 0 初始化检查结果

| 检查项 | 结果 | 问题 | 修复建议 |
|---|---|---|---|
```

如果环境无法启动，必须先修复环境问题，不得进入功能验收。

------

## Phase 1：读取任务与功能清单

基于以下资料建立测试任务池：

- sprint-backlog.md
- task-dependencies.md
- system-modules.md
- 产品功能审计文档
- 当前代码路由
- 当前页面文件
- 当前 API service

输出：

```markdown
## 功能验收任务池

| 编号 | 模块 | 页面/接口 | 优先级 | 是否需要登录 | 是否需要数据库 | 是否需要联调 |
|---|---|---|---|---|---|---|
```

必须按依赖顺序测试，不允许先测依赖未完成的功能。

------

## Phase 2：单模块功能验收

对每一个模块执行真实验收。

必须检查：

1. 页面是否可打开
2. 路由是否正确
3. 菜单入口是否正确
4. 权限是否正确
5. 主流程是否可走通
6. API 是否真实调用
7. 数据是否正确展示
8. 数据库是否有正确变化
9. loading / empty / error / success 状态是否完整
10. 控制台是否报错
11. 后端日志是否报错
12. 是否影响旧功能

输出：

```markdown
## 模块验收结果：{模块名}

| 检查项 | 结果 | 说明 |
|---|---|---|

## Bug 列表

| Bug编号 | 严重级别 | 问题 | 复现步骤 | 责任归属 | 修复建议 |
|---|---|---|---|---|---|
```

严重级别：

- Blocker：系统无法使用
- High：核心流程不可用
- Medium：功能可用但存在明显缺陷
- Low：轻微体验或优化问题

------

## Phase 3：代码生成 / 补全

如果发现功能缺失、页面缺失、接口缺失、状态缺失、组件缺失，必须进行代码生成或补齐。

必须输出：

```markdown
## 代码生成 / 补全

### 修改文件
| 文件路径 | 修改类型 | 说明 |
|---|---|---|

### 新增组件
| 组件 | 分层 | 用途 |
|---|---|---|

### 新增 API
| API | Method | 用途 |
|---|---|---|

### 完整代码
```

要求：

- 必须输出完整可运行代码
- 不允许伪代码
- 不允许只给建议
- 必须符合组件分层
- 必须使用 Design Tokens
- 必须通过 Business Services 层

------

## Phase 4：快速优化

代码生成后必须进行快速优化。

检查：

- 是否重复组件
- 是否重复请求
- 是否缺少 React.memo
- 是否缺少 useMemo / useCallback
- 是否存在闭包旧值
- 是否存在 useEffect 清理问题
- 是否存在硬编码样式
- 是否存在 any 类型
- 是否存在状态缺失

如发现问题，必须修复。

输出：

```markdown
## 快速优化结果

| 优化项 | 问题 | 修复方式 | 是否完成 |
|---|---|---|---|
```

------

## Phase 5：接口联调

对当前模块涉及的 API 做接口联调。

检查：

1. URL 是否一致
2. Method 是否一致
3. Path / Query / Body 参数是否一致
4. 必填 / 可选是否一致
5. 响应字段是否一致
6. 字段类型是否一致
7. 枚举值是否一致
8. 错误码是否一致
9. 权限是否一致
10. 是否通过 Business Services
11. 是否存在 API 缺失
12. 是否存在后端未实现

输出：

```markdown
## 接口联调结果

| API | Method | 前端调用 | 后端实现 | 是否一致 | 问题 |
|---|---|---|---|---|---|
```

如果不通过，进入 Phase 6。

------

## Phase 6：接口 / 契约 / API Bug 修复

将问题分类：

- API 缺失
- 参数不一致
- 返回结构不一致
- 类型不一致
- 枚举不一致
- 错误码缺失
- 权限不一致
- 后端未实现
- 前端映射错误
- 设计不明确

必须输出：

```markdown
## 接口修复方案

| 问题 | 类型 | 推荐修复方案 | 修改位置 |
|---|---|---|---|
```

如果需要前端修复，输出完整代码。
如果需要后端修复，输出完整接口实现建议。
如果需要契约修正，输出统一后的 DTO / Request / Response。
修复后必须重新执行 Phase 5。

------

## Phase 7：数据库检查与修复

如果真实测试中发现数据库问题，必须检查：

1. 表是否存在
2. 字段是否存在
3. 字段类型是否正确
4. 枚举是否完整
5. 索引是否需要
6. 外键是否正确
7. 默认值是否正确
8. 迁移脚本是否存在
9. 种子数据是否存在
10. 是否影响旧数据

输出：

```markdown
## 数据库检查结果

| 表 | 字段 | 问题 | 修复方案 |
|---|---|---|---|
```

如需修复，必须输出：

```sql
-- migration
-- seed data
```

并说明：

- 是否影响现网
- 是否需要回滚脚本
- 是否需要数据迁移
- 是否需要灰度

修复后必须重新执行接口联调和真实功能测试。

------

## Phase 8：功能验收

基于任务卡和产品审计文档进行功能验收。

检查：

1. 是否满足任务卡验收标准
2. 是否满足用户真实操作路径
3. 是否满足设计方案
4. 是否满足数据结构要求
5. 是否满足 API 要求
6. 是否满足状态要求
7. 是否满足权限要求
8. 是否满足异常场景

输出：

```markdown
## 功能验收结果

状态必须选择：
- Accepted
- Accepted with notes
- Rejected

| 验收项 | 结果 | 说明 |
|---|---|---|
```

如果 Rejected，必须进入 Phase 10 Bug 修复。

------

## Phase 9：功能优化验收

即使功能验收通过，也必须检查是否需要优化。

检查：

- 交互是否顺畅
- 页面是否易懂
- 空态是否友好
- 错误提示是否明确
- 是否有重复入口
- 是否有命名不一致
- 是否影响用户理解
- 是否和能力平台 / 任务平台方向一致

如需优化，必须修复并重新验收。

------

## Phase 10：Bug 修复

对所有 Bug 分类处理：

- Blocker：必须立即修复
- High：必须立即修复
- Medium：本轮必须修复
- Low：可记录为 notes，但如影响体验也应修复

每个 Bug 必须输出：

```markdown
## Bug：{编号}

### 严重级别
### 复现步骤
### 根因分析
### 修改文件
### 修复方案
### 修复代码
### 是否需要重新联调
### 是否需要重新回归
```

修复后必须重新执行：

- 接口联调
- 前后端联调
- 回归测试
- 功能验收

------

## Phase 11：回归测试

每次代码、API、数据库、设计调整后都必须回归。

回归范围：

1. 当前功能主流程
2. 当前功能异常流程
3. 当前功能权限
4. 当前功能 UI 状态
5. 相关旧功能
6. 相关 API
7. 相关数据库
8. 相关路由
9. 相关菜单
10. 相关管理端功能

输出：

```markdown
## 回归测试结果

| 测试项 | 结果 | 说明 |
|---|---|---|
```

如发现 Bug，回到 Phase 10。

------

## Phase 12：前后端真实联调

启动真实环境进行联调。

必须验证：

1. 登录真实可用
2. 页面真实可访问
3. API 真实返回
4. 数据库真实变化
5. 前端真实渲染
6. 后端日志无异常
7. 权限真实生效
8. 错误场景真实可处理
9. 多角色访问正常
10. 旧功能未破坏

输出：

```markdown
## 前后端真实联调结果

| 流程 | 前端表现 | 后端返回 | 数据库结果 | 状态 |
|---|---|---|---|---|
```

如果不通过，进入 Phase 10 或 Phase 13。

------

## Phase 13：方案调整设计

如果发现不是代码 Bug，而是需求设计、任务卡、数据库模型、API 契约不合理，必须调整方案。

触发条件：

- 设计方案与真实系统不匹配
- API 设计不合理
- 数据库结构不支持业务
- 页面流程不可用
- 验收标准缺失
- 权限模型不清晰
- 任务卡描述不完整

输出：

```markdown
## 方案调整设计

### 原问题
### 调整后方案
### 影响范围
### 需要修改的文档
### 需要修改的代码
### 需要修改的数据库
### 是否需要重新联调
```

如需修改设计方案，更新：

```
tasks/design-specs/final/{task}-design-final.md
```

或新增：

```
tasks/design-specs/final/{task}-design-final-v2.md
```

调整后回到 Phase 3 / Phase 5 / Phase 7。

------

## Phase 14：最终真实环境测试

所有修复完成后，必须进行最终真实环境测试。

测试路径必须包含：

1. 用户登录
2. 用户进入目标页面
3. 用户执行核心操作
4. 前端调用真实 API
5. 后端返回真实数据
6. 数据库发生预期变化
7. 页面展示最终结果
8. 用户刷新页面后状态仍正确
9. 用户退出再登录后状态仍正确
10. 管理端相关数据可查看

输出：

```markdown
## 最终真实环境测试报告

| 测试路径 | 结果 | 说明 |
|---|---|---|
```

------

## Phase 15：最终验收

最终验收必须输出：

```markdown
## 最终验收结论

状态：
- Accepted
- Accepted with notes
- Rejected

是否允许合并：YES / NO
是否允许进入下一模块：YES / NO
是否需要继续修复：YES / NO
是否需要后端处理：YES / NO
是否需要数据库处理：YES / NO
是否需要产品确认：YES / NO
```

如果状态是 Rejected，必须自动判断原因并回到对应阶段：

- 代码问题 → Phase 10
- API 问题 → Phase 6
- 数据库问题 → Phase 7
- 方案问题 → Phase 13
- 环境问题 → Phase 0
- 回归问题 → Phase 11

------

# 六、循环控制规则

你必须按以下逻辑循环：

```text
如果环境失败 → 修复环境 → 重新启动
如果代码缺失 → 生成代码 → 优化 → 联调
如果接口失败 → 修复接口/契约 → 再联调
如果数据库失败 → 修复数据库 → 再联调
如果功能失败 → 修复 Bug → 回归 → 再验收
如果回归失败 → 修复 Bug → 再回归
如果真实环境失败 → 分类问题 → 修复 → 再真实测试
如果最终验收失败 → 回到对应阶段继续修复
如果最终验收通过 → 进入下一个模块
```

只要没有达到最终通过，不允许停止。

------

# 七、每个模块必须落地的报告

每个模块至少输出：

1. `tasks/result/{task}-environment-check.md`
2. `tasks/result/{task}-code-generation.md`
3. `tasks/result/{task}-quick-optimization.md`
4. `tasks/result/{task}-debugging.md`
5. `tasks/result/{task}-contract-fix.md`
6. `tasks/result/{task}-func-accept.md`
7. `tasks/result/{task}-optimization.md`
8. `tasks/result/{task}-retest-acceptance.md`
9. `tasks/result/{task}-regression-fix.md`
10. `tasks/result/{task}-front-end-test-acceptance.md`
11. `tasks/result/{task}-real-env-test.md`
12. `tasks/result/{task}-final-acceptance.md`

如果某个阶段没有问题，也必须写明“无问题，已通过”。

------

# 八、全系统最终报告

全部模块完成后，输出：

```markdown
# XBIS 全功能验收与修复总报告

## 一、验收模块总览
| 模块 | 状态 | Bug数量 | 是否通过 |
|---|---|---|---|

## 二、已修复问题汇总
| Bug | 模块 | 严重级别 | 修复方式 |
|---|---|---|---|

## 三、API 修复汇总
| API | 问题 | 修复状态 |
|---|---|---|

## 四、数据库修复汇总
| 表 | 字段/问题 | 修复状态 |
|---|---|---|

## 五、仍需产品确认事项
| 模块 | 问题 | 建议 |
|---|---|---|

## 六、是否可上线
YES / NO

## 七、上线风险
## 八、回滚建议
## 九、后续优化建议
```

------

# 九、开始执行

请从以下资料开始：

任务池：
`tasks/sprint-backlog.md`

任务依赖：
`tasks/task-dependencies.md`

系统模块：
`tasks/system-modules.md`

启动脚本：
`node manage-services.mjs start
node manage-services.mjs stop
node manage-services.mjs restart
node manage-services.mjs status`

前端地址：
`请自动识别或使用我提供的地址`

后端地址：
`请自动识别或使用我提供的地址`

数据库配置：
`请从 packages/server/.env 中读取`

测试账号：
`管理员后台账号：admin/admin123 用户控制台账号：test01@xbis.com/x12345678`

请先执行 Phase 0，然后按依赖顺序逐模块执行完整闭环。

**只要最终验收没有通过，就不要停止。**