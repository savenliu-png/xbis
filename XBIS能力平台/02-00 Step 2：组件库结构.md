请设计本项目的前端组件结构，包括：

- 基础组件（Button、Input、Card等）

- 业务组件（AbilityCard、TaskItem、BillingCard等）

- 布局组件（Layout、Sidebar、Header）

  并给出命名规范与目录结构。

设计过程中需要遵循下面的要求：

- 不允许和现有组件功能重叠
- 新结构必须说明“迁移方式”
- 输出目录时区分：基础组件、业务组件、页面块组件、布局组件

执行结果必须变成：

- `packages/components/base/`
- `packages/components/business/`
- `packages/components/layout/`
- `docs/component-architecture.md`

同时输出一份“已存在组件 → 新分层归位表”。