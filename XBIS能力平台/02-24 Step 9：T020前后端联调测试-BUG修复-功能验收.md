你必须严格遵守本项目的：

1. Design Tokens（docs/design-tokens.md）
2. 组件库结构（docs/component-architecture.md）
3. 页面模板（docs/page-templates.md）
4. 开发规则文档（docs/dev-rules.md）
5. 标准样板页面（docs/page-templates.md）
6. 当前任务输入模板（docs/task-template.md）
7. Business Services 稳定业务接入层（./xbis/docs/XBIS_EXTERNAL_SYSTEM_INTEGRATION_GUIDE.md）

------

你现在扮演：

**资深前后端联调负责人 + QA测试负责人 + Bug修复负责人 + 产品验收官**

# 目标

基于当前任务的前端实现、后端接口、任务卡、设计方案、回归报告，执行完整的：

**前后端联调测试 → 问题定位 → Bug修复 → 复测 → 功能验收**

# 输入

请基于以下材料执行：

1. 当前任务卡（tasks/T020-plan-purchase-flow.md）
2. 当前设计方案 Final（tasks/design-specs/final/T020-plan-purchase-flow-design-final.md）
3. 前端代码实现
4. 后端接口文档 / OpenAPI / Mock / 实际接口
5. TypeScript 类型定义
6. D1 联调报告（tasks/result/T020-plan-purchase-flow-debugging.md）
7. D2 验收报告（tasks/result/T020-plan-purchase-flow-func-accept.md）
8. 回归测试报告（tasks/result/T020-plan-purchase-flow-retest-acceptance.md）
9. 修改文件列表

# 强制原则

- 不允许只看前端代码，必须核对后端接口契约
- 不允许跳过新增接口
- 不允许跳过字段命名、类型、枚举、状态流检查
- 不允许把 Mock 通过等同于真实联调通过
- 发现问题必须判断责任归属：前端 / 后端 / 契约 / 设计
- 对前端问题必须给出修复代码
- 对后端问题必须给出接口修改建议
- 修复后必须重新复测
- 存在 Blocker / High 问题时，不允许给出 Accepted

# 1. 联调范围确认

请先输出本次需要联调的内容：

| 类型 | 对象 | 是否新增 | 风险等级 | 是否必须联调 |
| ---- | ---- | -------- | -------- | ------------ |
|      |      |          |          |              |

必须覆盖：

- 页面入口
- API/service
- 请求参数
- 响应结构
- 错误码
- 类型定义
- 状态流
- 权限
- 降级逻辑
- 旧接口兼容

# 2. API契约核对

逐个接口核对：

| API  | Method | 前端调用 | 后端契约 | 请求参数是否一致 | 响应字段是否一致 | 状态 |
| ---- | ------ | -------- | -------- | ---------------- | ---------------- | ---- |
|      |        |          |          |                  |                  |      |

检查内容：

- URL 是否一致
- Method 是否一致
- Path 参数是否一致
- Query 参数是否一致
- Body 字段是否一致
- 字段命名是否一致
- 类型是否一致
- 枚举值是否一致
- 必填/可选是否一致
- 响应包裹结构是否一致
- 错误码是否一致

# 3. 数据与类型核对

请核对：

- 前端 TypeScript 类型是否匹配后端 DTO
- 是否存在前端字段但后端没有
- 是否存在后端字段但前端未处理
- 是否存在类型冲突
- 是否存在枚举不一致
- 是否存在数值单位不一致，例如百分比、金额、时间戳

输出：

| 字段 | 前端定义 | 后端定义 | 问题 | 修复建议 |
| ---- | -------- | -------- | ---- | -------- |
|      |          |          |      |          |

# 4. 主流程联调测试

按用户路径执行测试：

1. 打开页面
2. 加载数据
3. 执行核心操作
4. 提交请求
5. 后端返回结果
6. 前端更新状态
7. 用户看到结果

输出：

| 流程 | 操作 | 预期结果 | 实际结果 | 状态 |
| ---- | ---- | -------- | -------- | ---- |
|      |      |          |          |      |

# 5. 异常与边界测试

必须测试：

- 接口 400
- 接口 401 / 403
- 接口 500
- 网络超时
- 空数据
- 字段缺失
- 枚举未知值
- 权限不足
- 重复提交
- 金额 / 数值边界
- 时间格式异常

输出：

| 场景 | 后端返回 | 前端表现 | 是否通过 |
| ---- | -------- | -------- | -------- |
|      |          |          |          |

# 6. Bug列表

如果发现问题，输出：

| Bug编号 | 严重级别 | 问题描述 | 复现步骤 | 责任归属 | 影响范围 | 修复建议 |
| ------- | -------- | -------- | -------- | -------- | -------- | -------- |
|         |          |          |          |          |          |          |

严重级别：

- Blocker：阻塞上线
- High：核心流程异常
- Medium：边界/体验问题
- Low：轻微问题

责任归属：

- 前端
- 后端
- 契约不一致
- 设计不明确
- 待产品确认

# 7. Bug修复

对所有 Blocker / High / Medium 问题必须处理。

## 对前端问题

请输出：

- 问题原因
- 修改文件
- 修复方案
- 完整修复代码

## 对后端问题

请输出：

- 问题原因
- 需要修改的接口
- 推荐 DTO / 参数 / 返回结构
- 示例请求
- 示例响应

## 对契约问题

请输出：

- 当前冲突
- 推荐统一方案
- 需要同步修改的前端/后端/文档位置

# 8. 修复后复测

修复后重新执行：

- 原 Bug 复测
- 主流程联调
- API契约复测
- 异常场景复测
- 旧功能回归

输出：

| 测试项 | 结果 | 说明 |
| ------ | ---- | ---- |
|        |      |      |

# 9. 功能验收

基于任务卡验收标准输出最终验收：

| 验收项 | 状态 | 说明 |
| ------ | ---- | ---- |
|        |      |      |

最终状态必须选择其一：

- Accepted
- Accepted with notes
- Rejected

# 10. 最终结论

请输出：

- 是否允许合并：YES / NO
- 是否允许进入下一任务：YES / NO
- 是否需要继续修复：YES / NO
- 是否需要后端继续处理：YES / NO
- 是否需要产品确认：YES / NO

# 输出格式：文档落地到tasks/result/*-front-end-test-acceptance.md

请最终输出：

## 一、联调范围

## 二、API契约核对

## 三、数据与类型核对

## 四、主流程联调

## 五、异常与边界测试

## 六、Bug列表

## 七、Bug修复内容

## 八、复测结果

## 九、功能验收结论

## 十、是否允许合并
